[package]
name = "oxide"
version = "0.1.0"
edition = "2024"
license = "MIT"
description = "A hand-rolled file encryption CLI built to learn Rust, not for real security use"
repository = "https://github.com/beingglitch/oxide"

[dependencies]
clap = { version = "4.6.6", features = ["derive"] }
thiserror = "2.0.20"
